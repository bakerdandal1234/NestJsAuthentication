import { Injectable } from '@nestjs/common';

/**
 * Reserved for app-wide, non-domain-specific logic.
 * Currently unused — business logic lives in feature modules (auth, users).
 */
@Injectable()
export class AppService {}
